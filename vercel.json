{
  "buildCommand": "npm run build -w @sculpt/spec && npm run build -w @sculpt/db && npm run build -w @sculpt/chisel",
  "outputDirectory": "packages/chisel/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
