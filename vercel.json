{
  "buildCommand": "cd ../.. && npm run build -w @sculpt/spec && npm run build -w @sculpt/db && npx next build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
