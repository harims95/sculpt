# @sculpt/spec

The Sculpture Spec schema, TypeScript types, and validator.

## Status: 🚧 Needs Contributors

This package needs:

- [ ] TypeScript type definitions for the Sculpture Spec
- [ ] JSON Schema file for validation
- [ ] `validateSpec()` function that checks all 7 validation rules
- [ ] `createEmptySpec()` helper that returns a blank valid spec
- [ ] `diffSpecs()` function to compare two spec versions (for Rock Dust)
- [ ] Unit tests

## Quick Start

```bash
cd packages/spec
npm install
npm test
```

## See Also

- [Spec Schema Documentation](../../docs/SPEC_SCHEMA.md)
